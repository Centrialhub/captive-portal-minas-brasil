import { useEffect } from "react";
import App from "../App";

export default function IndexRoute() {
  useEffect(() => {
    // This route is a legacy placeholder to satisfy the visual-edit target.
    // The main entry point is defined in main.tsx.
  }, []);

  return <> <App /></>;
}
